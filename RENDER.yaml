services:
  - type: web
    name: youth-labour-market-stress
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:server --bind 0.0.0.0:$PORT --workers 1 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0